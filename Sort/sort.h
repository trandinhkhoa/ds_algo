#include <vector>

void bubbleSort(std::vector<int>& array);
// TODO: add begin and end argument for sorts
void mergeSort(std::vector<int>& array);
void quickSort(std::vector<int>& array, int begin, int end);