#include <list>

namespace cracking {
namespace linkedLists {

// TODO: work with <T> instead of <int>
void removeDups(std::list<int>& iList);
void removeDups_withoutBuffer(std::list<int>& iList);

}
}