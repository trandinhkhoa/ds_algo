#include <string>
// forward declration does not work if you want to use object/reference instead of pointer to the object

namespace cracking {

namespace arraysAndStrings {

bool hasAllUniqueCharacter(std::string& iStr);

}

}